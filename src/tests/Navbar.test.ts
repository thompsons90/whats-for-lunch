// import HamburgerNav from "../navbar/Navbar";
import { describe, expect, it } from "vitest";

// TODO: Create a test that checks for all links to be working
// import { render, screen } from "@testing-library/react";
// import userEvent from "@testing-library/user-event";
// import HamburgerNav, { NavItems } from "./HamburgerNav";

// describe("HamburgerNav", () => {
//   const navItems: NavItem[] = [
//     { title: "Home", path: "/" },
//     { title: "About", path: "/about" },
//     { title: "Contact", path: "/contact" },
//   ];

//   it("renders all nav items", () => {
//     render(<HamburgerNav navItems={navItems} />);

//     navItems.forEach((item) => {
//       const link = screen.getByRole("link", { name: item.title });
//       expect(link).toHaveAttribute("href", item.path);
//     });
//   });

//   it("opens and closes the menu on click", () => {
//     render(<HamburgerNav navItems={navItems} />);

//     const button = screen.getByRole("button", { name: "modal" });
//     userEvent.click(button);

//     const menu = screen.getByRole("navigation", { name: "nav-menu" });
//     expect(menu).toHaveClass("open");

//     userEvent.click(button);
//     expect(menu).toHaveClass("close");
//   });
// });

// describe("#hamburgerNav", () => {
//   it("navigates to the correct pages", () => {
//     expect().toBe(0);
//   });

//   //     it("returns 0 with no numbers", () => {
//   //     expect(difference()).toBe(0);
//   //   });

//   // it("returns same number with one number", () => {
//   //   expect(difference(2)).toBe(2);
//   // });

//   // it("returns difference with multiple numbers", () => {
//   //   expect(difference(1, 2, 3)).toBe(6);
//   // });
// });

import { memo, useState } from "react";

const MobileRecipeDirections = memo(() => {
  return (
    <div>
      <h2>Welcome to the Directions:</h2>
    </div>
  );
});

export default MobileRecipeDirections;
